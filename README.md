This is a example project to demonstrate the mutant gem at the ruby user group cologne (cologne.rb)

The code is obviously overdrawn to show the point. ;)

Have fun with it by running ```rake mutant```